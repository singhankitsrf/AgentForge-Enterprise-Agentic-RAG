# Contributing

1. Create a focused branch.
2. Add or update tests and evaluation cases with behavior changes.
3. Run `ruff check .`, `pytest`, and `python scripts/evaluate.py`.
4. Do not add unsupported performance claims.
5. Document new side-effecting tools in the policy layer and require approval by default.
